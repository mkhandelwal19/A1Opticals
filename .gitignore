node_modules/
.env
.env.*
*.log
Thumbs.db
.DS_Store

# Signed agreement stays local; the repo is public.
contract/

# The API's SQLite database lives beside the code but never in the repo.
api/data/

# Internal pricing and scope working draft. Stays on disk; never in the public
# repo — it contains proposed prices and margin reasoning.
PROPOSAL.md
